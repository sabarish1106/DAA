#include <stdio.h>

void exampleAlgorithm1(int n) {
    for (int i = 0; i < n; i++) {
        printf("Example Algorithm 1\n");
    }
}

void exampleAlgorithm2(int n) {
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            printf("Example Algorithm 2\n");
        }
    }
}

int main() {
    int inputSize = 5;

    // Example Algorithm 1
    exampleAlgorithm1(inputSize);

    // Example Algorithm 2
    exampleAlgorithm2(inputSize);

    return 0;
}
